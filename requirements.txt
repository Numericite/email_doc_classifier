# Dépendances de l'application 
anthropic==0.116.0         # analyse des documents (API Claude)
docling==2.107.0           # extraction du texte (PDF / DOCX + OCR RapidOCR)
exchangelib==5.6.0         # surveillance de la boîte mail Exchange
psycopg[binary]==3.3.4     # PostgreSQL — driver SQL direct ; [binary] = wheel précompilé
PySide6==6.11.1            # interface de bureau (Qt)
python-dotenv==1.2.2       # chargement de la configuration (.env)
requests==2.34.2           # appels HTTP : Nextcloud (WebDAV) + Ricobot
